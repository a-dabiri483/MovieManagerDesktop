<Window x:Class="MovieManagerDesktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:viewmodels="clr-namespace:MovieManagerDesktop.ViewModels"
        xmlns:views="clr-namespace:MovieManagerDesktop.Views"
        xmlns:controls="clr-namespace:MovieManagerDesktop.Controls"
        Title="Movie Collection Dashboard" Height="750" Width="1250"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen"
        FontFamily="{StaticResource AppFontFamily}"
        FlowDirection="RightToLeft"
        MouseMove="Window_MouseMove">

    <Window.DataContext>
        <viewmodels:MainViewModel />
    </Window.DataContext>

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="6" CornerRadius="16" GlassFrameThickness="0" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:HomeViewModel}">
            <views:HomeView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:ScanViewModel}">
            <views:ScanView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:FavoritesViewModel}">
            <views:MoviesView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:CollectionMoviesViewModel}">
            <views:MoviesView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:CollectionsViewModel}">
            <views:CollectionsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:PersonMoviesViewModel}">
            <views:MoviesView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:MoviesViewModel}">
            <views:MoviesView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:SettingsViewModel}">
            <views:SettingsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:MediaDetailsViewModel}">
            <views:MediaDetailsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:SeriesDetailViewModel}">
            <views:SeriesDetailView />
        </DataTemplate>

        <!-- Style for modern sidebar buttons -->
        <Style x:Key="SidebarRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="GroupName" Value="SidebarGroup"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryText}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Margin" Value="10,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="{StaticResource RadiusMd}" Padding="15,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="40" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                
                                <!-- Active Indicator Line -->
                                <Border x:Name="ActiveIndicator" Grid.ColumnSpan="3" HorizontalAlignment="Right" Width="3" CornerRadius="1.5" Background="{DynamicResource PrimaryColor}" Margin="0,10,-15,10" Opacity="0">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#00B4D8" BlurRadius="8" ShadowDepth="0" Opacity="0.8"/>
                                    </Border.Effect>
                                </Border>
                                
                                <materialDesign:PackIcon x:Name="icon" Grid.Column="0" Kind="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" 
                                                         Width="22" Height="22" VerticalAlignment="Center" Foreground="{TemplateBinding Foreground}" RenderTransformOrigin="0.5,0.5">
                                    <materialDesign:PackIcon.RenderTransform>
                                        <RotateTransform Angle="0"/>
                                    </materialDesign:PackIcon.RenderTransform>
                                </materialDesign:PackIcon>
                                
                                <ContentPresenter Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5,0,0,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                                <Setter Property="Background" Value="{DynamicResource HoverBackground}"/>
                                <Setter TargetName="ActiveIndicator" Property="Opacity" Value="1"/>
                                <Setter TargetName="icon" Property="Foreground" Value="{DynamicResource PrimaryColor}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource HoverBackground}"/>
                                <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="icon" Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" To="15" Duration="0:0:0.1" />
                                            <DoubleAnimation Storyboard.TargetName="icon" Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" To="-10" BeginTime="0:0:0.1" Duration="0:0:0.1" />
                                            <DoubleAnimation Storyboard.TargetName="icon" Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" To="0" BeginTime="0:0:0.2" Duration="0:0:0.1" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <materialDesign:DialogHost Identifier="RootDialog" DialogBackground="{DynamicResource MainBackground}">
        <Border CornerRadius="16" ClipToBounds="True">
            <Grid>
            <!-- Background Flat Color -->
            <Border Background="{DynamicResource MainBackground}" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="260" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- LEFT SIDEBAR -->
                <Border Grid.Column="0" BorderBrush="{DynamicResource LightBorder}" BorderThickness="0,0,1,0" Background="{DynamicResource SidebarBackground}">
                    <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="100" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="80" />
                    </Grid.RowDefinitions>

                    <!-- App Logo / Title -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="25,0" Cursor="Hand">
                        <StackPanel.InputBindings>
                            <MouseBinding Gesture="LeftClick" Command="{Binding NavigateToHomeCommand}" />
                        </StackPanel.InputBindings>
                        <Image x:Name="AppLogo" Source="pack://application:,,,/Assets/logo.png" Width="55" Height="55" RenderOptions.BitmapScalingMode="HighQuality">
                            <Image.Effect>
                                <DropShadowEffect Color="#FF533483" BlurRadius="10" ShadowDepth="0" Opacity="0.5"/>
                            </Image.Effect>
                        </Image>
                        <StackPanel Margin="15,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="مدیریت" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}"/>
                            <TextBlock Text="فیلم و سریال" FontSize="14" Foreground="{DynamicResource SecondaryText}"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Navigation Items -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" Margin="0,10">
                        <StackPanel>
                            <!-- Categories -->
                            <TextBlock Text="کتابخانه من" Foreground="{DynamicResource SecondaryText}" FontSize="12" FontWeight="SemiBold" Margin="25,20,0,5"/>
                            <RadioButton Style="{StaticResource SidebarRadioButtonStyle}" Tag="HomeOutline" Content="داشبورد" Command="{Binding NavigateToHomeCommand}" IsChecked="True"/>
                            <RadioButton Style="{StaticResource SidebarRadioButtonStyle}" Tag="MoviePlayOutline" Content="فیلم‌ها و سریال‌ها" Command="{Binding NavigateToMoviesCommand}" />
                            
                            <RadioButton Style="{StaticResource SidebarRadioButtonStyle}" Tag="HeartOutline" Content="علاقه‌مندی‌ها" Command="{Binding NavigateToFavoritesCommand}" />

                            <!-- Collections -->
                            <TextBlock Text="دسته‌بندی‌ها" Foreground="{DynamicResource SecondaryText}" FontSize="12" FontWeight="SemiBold" Margin="25,20,0,5"/>
                            <RadioButton Style="{StaticResource SidebarRadioButtonStyle}" Tag="AnimationPlayOutline" Content="مجموعه‌ها" Command="{Binding NavigateToCollectionsCommand}" />

                            <!-- Tracker -->
                            <TextBlock Text="سایر" Foreground="{DynamicResource SecondaryText}" FontSize="12" FontWeight="SemiBold" Margin="25,20,0,5"/>
                            <RadioButton Style="{StaticResource SidebarRadioButtonStyle}" Tag="BookmarkOutline" Content="لیست تماشا" Command="{Binding NavigateToWatchlistCommand}" />
                            
                            <!-- System -->
                            <TextBlock Text="سیستم" Foreground="{DynamicResource SecondaryText}" FontSize="12" FontWeight="SemiBold" Margin="25,20,0,5"/>
                            <RadioButton Style="{StaticResource SidebarRadioButtonStyle}" Tag="Harddisk" Content="اسکن خودکار درایوها" Command="{Binding NavigateToScanCommand}" />
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Bottom Settings -->
                    <RadioButton Grid.Row="2" Style="{StaticResource SidebarRadioButtonStyle}" Tag="CogOutline" Content="تنظیمات" Command="{Binding NavigateToSettingsCommand}"/>
                </Grid>
            </Border>

            <!-- RIGHT MAIN CONTENT AREA -->
            <Grid Grid.Column="1">
                
                <!-- Content Control connecting to ViewModels (Full Screen without Margins) -->
                <ContentControl Content="{Binding CurrentViewModel, NotifyOnTargetUpdated=True}" Margin="0">
                    <ContentControl.Style>
                        <Style TargetType="ContentControl">
                            <Style.Triggers>
                                <EventTrigger RoutedEvent="Binding.TargetUpdated">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.25"/>
                                            <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,30,0,0" To="0" Duration="0:0:0.25">
                                                <ThicknessAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseOut"/>
                                                </ThicknessAnimation.EasingFunction>
                                            </ThicknessAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Style.Triggers>
                        </Style>
                    </ContentControl.Style>
                </ContentControl>

                <!-- Custom Window Title Bar for Dragging and Controls -->
                <Border Panel.ZIndex="10" VerticalAlignment="Top" MouseLeftButtonDown="Window_MouseLeftButtonDown" Height="40" WindowChrome.IsHitTestVisibleInChrome="True">
                    <Border.Background>
                        <SolidColorBrush Color="Transparent" />
                    </Border.Background>
                    <Border.Resources>
                        <Style x:Key="WindowBtnStyle" TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{DynamicResource SecondaryText}"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
                            <Setter Property="FontSize" Value="10"/>
                            <Setter Property="Width" Value="45"/>
                            <Setter Property="Height" Value="30"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource HoverBackground}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                        <Style x:Key="WindowCloseBtnStyle" TargetType="Button" BasedOn="{StaticResource WindowBtnStyle}">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource ErrorColor}"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Resources>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                        <Button Content="&#xE921;" Style="{StaticResource WindowBtnStyle}" Click="BtnMinimize_Click"/>
                        <Button x:Name="BtnMaximize" Content="&#xE922;" Style="{StaticResource WindowBtnStyle}" Click="BtnMaximize_Click"/>
                        <Button Content="&#xE8BB;" Style="{StaticResource WindowCloseBtnStyle}" Click="BtnClose_Click"/>
                    </StackPanel>
                </Border>
            </Grid>
            
            <!-- Global Toasts overlay -->
            <controls:ToastControl Grid.ColumnSpan="2" VerticalAlignment="Top" HorizontalAlignment="Center" Panel.ZIndex="9999" Margin="0,30,0,0" />
            
        </Grid>
        </Grid>
    </Border>
    </materialDesign:DialogHost>
</Window>
