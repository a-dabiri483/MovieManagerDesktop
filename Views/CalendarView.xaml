<UserControl x:Class="MovieManagerDesktop.Views.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:MovieManagerDesktop.Converters"
             xmlns:controls="clr-namespace:MovieManagerDesktop.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             FlowDirection="RightToLeft">

    <UserControl.Resources>
        <converters:ZeroToVisibilityConverter x:Key="ZeroToVisibilityConverter" />
        <converters:StringNullOrEmptyToVisibilityConverter x:Key="StringNullOrEmptyToVisibilityConverter" />
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="20,50,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header: Month Navigation + Fetch Button -->
        <Grid Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Command="{Binding PreviousMonthCommand}" Style="{StaticResource MaterialDesignFloatingActionMiniSecondaryButton}" Margin="0,0,20,0" ToolTip="ماه قبل">
                <materialDesign:PackIcon Kind="ChevronRight" Width="24" Height="24"/>
            </Button>

            <TextBlock Grid.Column="1" Text="{Binding MonthName, Converter={StaticResource PersianNumberConverter}}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}" HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <Button Grid.Column="2" Command="{Binding NextMonthCommand}" Style="{StaticResource MaterialDesignFloatingActionMiniSecondaryButton}" Margin="20,0,20,0" ToolTip="ماه بعد">
                <materialDesign:PackIcon Kind="ChevronLeft" Width="24" Height="24"/>
            </Button>

            <!-- Fetch Button -->
            <Button Grid.Column="3" Command="{Binding FetchMonthContentCommand}" 
                    Style="{StaticResource MaterialDesignRaisedButton}" 
                    Background="{DynamicResource PrimaryColor}" Foreground="#050510" BorderThickness="0"
                    Height="40" Padding="15,0" materialDesign:ButtonAssist.CornerRadius="8"
                    IsEnabled="{Binding IsFetching, Converter={StaticResource InverseBooleanConverter}}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="CloudDownload" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="دریافت عناوین این ماه" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Fetching indicator -->
        <ProgressBar Grid.Row="1" Style="{StaticResource MaterialDesignLinearProgressBar}" IsIndeterminate="True" 
                     Visibility="{Binding IsFetching, Converter={StaticResource BoolToVis}}" Margin="0,0,0,10"/>

        <!-- Days of Week Header -->
        <UniformGrid Grid.Row="2" Columns="7" Margin="0,0,0,5" VerticalAlignment="Top">
            <TextBlock Text="شنبه" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource SecondaryText}" HorizontalAlignment="Center" Padding="0,5"/>
            <TextBlock Text="یکشنبه" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource SecondaryText}" HorizontalAlignment="Center" Padding="0,5"/>
            <TextBlock Text="دوشنبه" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource SecondaryText}" HorizontalAlignment="Center" Padding="0,5"/>
            <TextBlock Text="سه‌شنبه" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource SecondaryText}" HorizontalAlignment="Center" Padding="0,5"/>
            <TextBlock Text="چهارشنبه" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource SecondaryText}" HorizontalAlignment="Center" Padding="0,5"/>
            <TextBlock Text="پنجشنبه" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource SecondaryText}" HorizontalAlignment="Center" Padding="0,5"/>
            <TextBlock Text="جمعه" FontSize="13" FontWeight="Bold" Foreground="#FF5252" HorizontalAlignment="Center" Padding="0,5"/>
        </UniformGrid>

        <!-- Days Grid -->
        <ItemsControl Grid.Row="3" ItemsSource="{Binding Days}" Margin="0,5,0,0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="7" VerticalAlignment="Top"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="2" CornerRadius="8" MinHeight="80" Cursor="Hand"
                            Visibility="{Binding DayNumber, Converter={StaticResource ZeroToVisibilityConverter}, ConverterParameter=Inverse}">
                        <Border.InputBindings>
                            <MouseBinding Gesture="LeftClick" Command="{Binding DataContext.SelectDayCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" />
                        </Border.InputBindings>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="#10FFFFFF"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="BorderBrush" Value="#20FFFFFF"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsToday}" Value="True">
                                        <Setter Property="BorderBrush" Value="{DynamicResource PrimaryColor}"/>
                                        <Setter Property="BorderThickness" Value="2"/>
                                        <Setter Property="Background" Value="#2000E5FF"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsFriday}" Value="True">
                                        <Setter Property="BorderBrush" Value="#40FF5252"/>
                                    </DataTrigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#20FFFFFF"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        
                        <Grid>
                            <!-- Background Poster (semi-transparent) -->
                            <Image controls:CachedImage.SourceUrl="{Binding FirstPosterUrl}" Stretch="UniformToFill" Opacity="0.2"
                                   Visibility="{Binding FirstPosterUrl, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}">
                                <Image.Clip>
                                    <RectangleGeometry RadiusX="8" RadiusY="8" Rect="0,0,200,200"/>
                                </Image.Clip>
                            </Image>
                            
                            <StackPanel Margin="5">
                                <!-- Day Number -->
                                <TextBlock Text="{Binding DayNumber, Converter={StaticResource PersianNumberConverter}}" FontSize="15" FontWeight="SemiBold" HorizontalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsFriday}" Value="True">
                                                    <Setter Property="Foreground" Value="#FF5252"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                
                                <!-- Release Dots -->
                                <ItemsControl ItemsSource="{Binding Releases}" Margin="0,3,0,0">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Ellipse Width="6" Height="6" Margin="1">
                                                <Ellipse.Style>
                                                    <Style TargetType="Ellipse">
                                                        <Setter Property="Fill" Value="#FFFFC107"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding MediaType}" Value="Series">
                                                                <Setter Property="Fill" Value="#00E5FF"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Ellipse.Style>
                                            </Ellipse>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
