<Window x:Class="MovieManagerDesktop.Views.PosterSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:converters="clr-namespace:MovieManagerDesktop.Converters"
        xmlns:controls="clr-namespace:MovieManagerDesktop.Controls"
        mc:Ignorable="d"
        Title="انتخاب پوستر" Height="650" Width="800"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        FlowDirection="RightToLeft">

    <Window.Resources>
        <converters:ImageUrlToCachedImageConverter x:Key="ImageUrlToCachedImageConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <DataTemplate x:Key="PosterItemTemplate">
            <Border Margin="10" CornerRadius="10" ClipToBounds="True" Cursor="Hand" Width="150" Height="225">
                <Border.InputBindings>
                    <MouseBinding Gesture="LeftClick" Command="{Binding DataContext.SelectPosterCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}"/>
                </Border.InputBindings>
                <Grid>
                    <Image controls:CachedImage.SourceUrl="{Binding}" Stretch="UniformToFill"/>
                    <Border Background="#80000000" Opacity="0" x:Name="HoverOverlay">
                        <materialDesign:PackIcon Kind="CheckCircleOutline" Width="40" Height="40" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
                <Border.Triggers>
                    <EventTrigger RoutedEvent="MouseEnter">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="HoverOverlay" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                    <EventTrigger RoutedEvent="MouseLeave">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="HoverOverlay" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Border.Triggers>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Border BorderThickness="1" BorderBrush="{DynamicResource NormalBorder}" Margin="10" CornerRadius="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.3"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Backgrounds for Header and Content -->
            <Border Grid.Row="0" Background="{DynamicResource SidebarBackground}" CornerRadius="15,15,0,0"/>
            <Border Grid.Row="1" Background="{DynamicResource MainBackground}" CornerRadius="0,0,15,15"/>

            <!-- Header -->
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" Command="{Binding CancelCommand}" Style="{StaticResource MaterialDesignIconForegroundButton}" Foreground="{DynamicResource SecondaryText}" Width="40" Height="40">
                    <materialDesign:PackIcon Kind="Close" Width="24" Height="24"/>
                </Button>
                
                <TextBlock Grid.Column="1" Text="انتخاب پوستر جایگزین" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0"/>
            </Grid>
            <Separator VerticalAlignment="Bottom" Background="{DynamicResource NormalBorder}"/>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="10">
                <StackPanel>
                    <ItemsControl ItemsSource="{Binding Posters}" ItemTemplate="{StaticResource PosterItemTemplate}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel HorizontalAlignment="Center"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>

                    <!-- Load More Button -->
                    <Button Command="{Binding LoadMoreCommand}" 
                            Visibility="{Binding HasMorePosters, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Style="{StaticResource MaterialDesignRaisedButton}" 
                            Background="{DynamicResource PrimaryColor}"
                            Foreground="#050510"
                            Content="بارگیری بیشتر" 
                            Margin="0,20,0,20" 
                            HorizontalAlignment="Center" 
                            Width="200" Height="45"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
