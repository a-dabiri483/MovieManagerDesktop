<UserControl x:Class="MovieManagerDesktop.Controls.ToastControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:services="clr-namespace:MovieManagerDesktop.Services"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400"
             FlowDirection="RightToLeft">

    <!-- Bind to Singleton Service -->
    <ItemsControl ItemsSource="{Binding Source={x:Static services:ToastService.Instance}, Path=Toasts}">
        <!-- Container Panel to stack toasts -->
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="Vertical" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0"/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>

        <!-- Template for each ToastMessage -->
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Border Background="#F51A1D36" 
                        BorderBrush="{Binding BorderColor}" 
                        BorderThickness="1"
                        CornerRadius="12"
                        Margin="0,3"
                        Padding="10,6"
                        MinWidth="200"
                        MaxWidth="360">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="12" Opacity="0.55" ShadowDepth="2"/>
                    </Border.Effect>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Icon -->
                        <Border Width="28" Height="28" CornerRadius="14" Background="{Binding BackgroundColor}" VerticalAlignment="Center">
                            <materialDesign:PackIcon Kind="{Binding IconKind}" Foreground="{Binding BorderColor}" Width="16" Height="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        
                        <!-- Content -->
                        <StackPanel Grid.Column="1" Margin="10,0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="11.5" Foreground="White" Margin="0,0,0,2"/>
                            <TextBlock Text="{Binding Message}" FontSize="11" Foreground="#CBD5E1" TextWrapping="Wrap" LineHeight="16"/>
                        </StackPanel>
                        
                        <!-- Close Button -->
                        <Button Grid.Column="2" 
                                Style="{StaticResource MaterialDesignIconForegroundButton}" 
                                Width="22" Height="22" 
                                Padding="0" 
                                Opacity="0.6"
                                VerticalAlignment="Center"
                                Click="CloseButton_Click"
                                Tag="{Binding}">
                            <materialDesign:PackIcon Kind="Close" Width="12" Height="12" Foreground="White"/>
                        </Button>
                    </Grid>
                </Border>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
        
        <!-- Entrance Animation -->
        <ItemsControl.ItemContainerStyle>
            <Style TargetType="ContentPresenter">
                <Setter Property="RenderTransform">
                    <Setter.Value>
                        <TranslateTransform Y="-100"/>
                    </Setter.Value>
                </Setter>
                <Setter Property="Opacity" Value="0"/>
                <Style.Triggers>
                    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.3"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="0" Duration="0:0:0.4">
                                    <DoubleAnimation.EasingFunction>
                                        <QuadraticEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                    <DataTrigger Binding="{Binding IsClosing}" Value="True">
                        <DataTrigger.EnterActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.3"/>
                                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="-100" Duration="0:0:0.4">
                                        <DoubleAnimation.EasingFunction>
                                            <QuadraticEase EasingMode="EaseIn"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.EnterActions>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ItemsControl.ItemContainerStyle>
    </ItemsControl>
</UserControl>
